def encoding_function(message):
    message = list(message)
    import wheel_tables
    for character in message:
        char_type = type(character)
        if char_type == 'str':
            #code
    print(message)